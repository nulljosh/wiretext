name: Wiretext
options:
  bundleIdPrefix: com.nulljosh
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "1.0.2"
    CURRENT_PROJECT_VERSION: "1"

targets:
  Wiretext-iOS:
    type: application
    platform: iOS
    sources:
      - path: App
      - path: web
        buildPhase: resources
        type: folder
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.nulljosh.wiretext
        INFOPLIST_FILE: App/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        TARGETED_DEVICE_FAMILY: "1,2"
        # ponytail: no AppIcon asset catalog yet — generate one from icon.svg before App Store submission

schemes:
  Wiretext-iOS:
    build:
      targets:
        Wiretext-iOS: all
    run:
      config: Debug
    archive:
      config: Release
    shared: true
